name: kalantar_three_person
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: '>=3.35.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5
  image_picker: ^1.2.2
  file_picker: ^10.3.10
  record: ^6.1.2
  audioplayers: ^6.8.1
  path_provider: ^2.1.6
  flutter_secure_storage: ^10.3.4
  dio: ^5.11.1
  socket_io_client: ^3.1.6
  flutter_local_notifications: ^19.0.0
  flutter_timezone: ^4.1.0
  timezone: ^0.10.0
  flutter_svg: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/icons/
