# وضعیت خروجی کلانتر

- ساختار Flutter برای نسخه مستقل سه‌نفره بازطراحی شد.
- UI مشکی با رنگ‌های بنفش/قرمز/آبی و آیکون‌های خاکستری اضافه شد.
- ورود سه‌کلیدی با نمایش ناشناس اضافه شد.
- چت، رسانه، دفتر مشترک، احساسات و نیازهای مشترک و بازی‌های بی‌پایان اضافه شدند.
- APIهای check-in و فهرست رسانه به سرور اضافه شدند.
- schema برای نگهداری وضعیت احساسات/نیازها اضافه شد.
- label عمومی روی سرور به «ناشناس» محدود شد.
- پیکربندی Railway و Volume برای ماندگاری رسانه مستند شد.

محیط فعلی این بسته Flutter SDK نصب‌شده ندارد؛ بنابراین APK/Android release در همین محیط ساخته نشده است. ZIP شامل سورس کامل برای `flutter pub get` و `flutter build apk --release` در محیط Flutter است.
